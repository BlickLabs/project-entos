server {
  listen 80;
  listen [::]:80;

  root /home/static/project-entos;
  index index.html index.htm;

  server_name www.sandbox.entos.getmore.mx sandbox.entos.getmore.mx;

  location / {
      try_files $uri $uri/ =404;
  }
}
